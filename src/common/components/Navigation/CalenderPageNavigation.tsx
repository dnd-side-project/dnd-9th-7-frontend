import { Navigation } from '@common/components/Navigation';

export const CalendarPageNavigation = () => {
  return (
    <Navigation>
      <Navigation.Left>
        <span>MUSIC LOG</span>
      </Navigation.Left>
    </Navigation>
  );
};
